Snapshot of the TanStack analysis store (sessions.json) and the Spyglass / Sep 22 range source files, copied here so the numbers survive a sandbox wipe.

The live PWA reads `../spyglass-2026-09-15.json` + `coach-feed.json`, not this folder.
Cumulative rolling averages still come from bay sessions only. Spyglass is `kind: on-course` in the rewrite store and `sim:true` on the PWA card — either way it does not mix into remaining/best-5.
